[package]
name = "sval"
version = "0.0.0"
edition = "2021"

[workspace]
members = [
    "coroutine",
    "derive",
    "json",
    "js",
    "fmt",
    "serde",
    "valuable",
    "erased",
    "buffer",
    "json/benches/twitter",
    "json/benches/profile",
    "json/tests/twitter",
    "examples/browser",
    "examples/simple"
]

[features]
std = ["alloc"]
alloc = []

[profile.release]
debug = true
opt-level = 3
lto = "fat"
codegen-units = 1
