[package]
name = "sval-test"
version = "0.0.0"
edition = "2021"

[features]
alloc = ["sval/alloc"]
std = ["sval/std"]

[dependencies.sval]
path = "../"

[dev-dependencies.num-bigint]
version = "~0.4"

[dev-dependencies.num-traits]
version = "0.2"
